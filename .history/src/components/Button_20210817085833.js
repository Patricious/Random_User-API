import React from 'react'

const Button = ({ isActive, clicked }) => {
    return ( <
        div >
        <
        button onscl > Get User < /button> < /
        div >
    )
}

export default Button